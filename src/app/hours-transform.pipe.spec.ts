import { HoursTransformPipe } from './hours-transform.pipe';

describe('HoursTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new HoursTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
